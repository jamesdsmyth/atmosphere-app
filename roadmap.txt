- update colors
- fix tests
- auto fresh every 30 minutes
- night time if clear will show a moon
- user chooses whether they like the color or not using Firebase

so now I need to wrap the store using a connect in my weather list class.
this is because we have already provided it by wrapping it in the main app.